"use strict";

import {SimplyBuilderAbstractInterface} from "./interface.js";

const name = "AbstractSimplyBuilderCoreModule";
const version = "1.0.0";

export const SimplyBuilderAbstract = Object.freeze({
    name, version, instance: SimplyBuilderAbstractInterface
});